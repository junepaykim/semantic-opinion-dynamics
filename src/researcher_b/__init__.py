"""Researcher B workspace.

Owns:
- Embeddings + semantic polarization metrics
- Visualization (PCA/t-SNE, variance over time)

Recommended entry point:
- Read runs/<run_id>/steps/*.json produced by the engine.
"""
